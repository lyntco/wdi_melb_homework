<div class="container">
  <h2>show details</h2>

  <% @result.each do |row| %>
  <p><%= row['title'] %></p>
  <p><%= row['description'] %></p>
  <p><%= row['genre'] %></p>
  <a href="/videos/<%= row['id'] %>/edit"><button class="btn btn-primary">Edit</button></a>
  <% end %>

  <a href="/"><button class="btn btn-default">Home</button></a>
</div>
