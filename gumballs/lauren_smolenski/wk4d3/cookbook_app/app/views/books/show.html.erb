<h1><%= @book.title %></h1>

<img src="<%= @book.image_url %>" alt="">
<p>By <%= @book.chef %></p>
<p><%= link_to 'edit', edit_book_path(@book) %></p>
<p><%= link_to 'delete', @book, :method => :delete, :data => { :confirm => 'are you sure you want to delete this book?' } %></p>
<%= link_to 'view all recipes in this book', "/books/#{@book.id}/recipes" %>
<%= link_to 'add a recipe to this book', new_recipe_path %>