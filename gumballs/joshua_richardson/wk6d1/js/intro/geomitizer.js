var radius = 10;
var circumference =  2 * (Math.PI * radius);

var result =  + circumference;


var area = (radius * radius * Math.PI);

var result2 = The area of the circle is " + (radius * radius * Math.PI);