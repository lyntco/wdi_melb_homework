var topChoices = ["blue", "Blairyyyy", "WE"]
var topChoicesLength = topChoices.length

for (var i = 0; i < topChoicesLength; i++) {
  numChoices = "My " + (i + 1) + "#" + " choice is " + topChoices[i];
  console.log(numChoices);
}
