<div class="container">
  <h1>RECIPES</h1><br>

  <% @recipes.each do |recipe| %>
    <p><h3><%= link_to recipe.name, recipe_path(recipe.name) %></h3></p>
    <p><%= link_to (image_tag(recipe.image_url)), recipe_path(recipe.name)  %></p>
    <%#= link_to 'delete', recipe, :method => :delete, :data => { :confirm => 'are you sure?' }, class: "btn btn-default"  %>
    <%#= link_to 'edit', edit_recipe_path(recipe.id), class: "btn btn-default" %>
  <% end %>

</div>  